{
  "scripts": {
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "npx tsc -p tsconfig.json",
    "watch": "npx tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.4",
    "typescript": "^5.5.4"
  }
}
